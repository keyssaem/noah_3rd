# 인공지능 로봇과 올바른 관계 맺기 시뮬레이터
### AI 전학생 '노아'와 함께하는 공존·상생 프로젝트

**성취기준 [6도02-03]** 인간과 인공지능 로봇 간의 다양한 관계를 파악(도구화 관계 vs 존중의 관계)하고,
도덕에 기반을 둔 관계 형성의 필요성을 탐구한다. (수미상관 질문을 통한 열린 결말)

---

## 🚀 실행 방법

### 방법 1 — 그냥 더블클릭 (가장 간단)
`index.html`을 더블클릭하면 바로 실행됩니다. (Three.js는 CDN에서 불러오므로 인터넷 연결 필요)
- 단, **얼굴 스캔의 실제 카메라 모드**는 보안 정책상 작동하지 않습니다 → 게임 안의 "상상 스캔 모드"를 선택하면 됩니다.

### 방법 2 — 로컬 서버 (카메라 스캔까지 전부 사용)
```bash
# 이 폴더에서 실행 (아래 중 하나)
npx http-server -p 8347
python -m http.server 8347
```
브라우저에서 `http://localhost:8347` 접속.

### 태블릿에서 플레이
같은 와이파이에서 PC의 IP로 접속하면 됩니다. 화면 좌측 하단 **조이스틱**, 우측 하단 **점프(⬆)/상호작용(F)** 버튼이 자동으로 나타납니다.

---

## 🎮 조작 방법
| 동작 | PC | 태블릿 |
|---|---|---|
| 이동 | `W A S D` / 방향키 | 왼쪽 조이스틱 |
| 점프 | `Space` | ⬆ 버튼 |
| 상호작용/대화 | `F` | F 버튼 |
| 대화 넘기기 | 클릭 / `Space` / `Enter` | 대화창 터치 |

---

## 🗺️ 게임 흐름 (스토리보드 구현)
1. **시작하기** — 메인 화면 → 이름/성별 설정(남·녀 3D 캐릭터, 포켓몬 스타일) → 집에서 기상
2. **월드맵** — 마리오 감성 점프 발판 + 연구소 트럭이 떨어뜨린 <로봇 3원칙> 카드 3장 수집 튜토리얼 → 동혁이의 등교 퀘스트
3. **도구화 에피소드 (거울1~5)** — 교실(노아 외형 선택 human/animal/car, 친구의 정의 입력, 수미상관 O/X ①, 얼굴 데이터 스캔) → 수학 대결(노아 필승) → 미술(자석처럼 끌리는 선택지) → 운동장(티볼 등급 분류 사건) → 복도(노아가 뒷담을 듣다)
4. **반전** — 가짜 엔딩 → 지지직(Vibration API) 시스템 오류 → 교수님의 3원칙 회상 → "도구화 100% / 존중 10%→1%" → 노아 강제 종료 → 비밀 코드 **7942(친구사이)** 재시작
5. **존중 에피소드 (거울1~5)** — 붉어진 월드맵에서 다시 시작 → 거절할 권리+개인정보 분류 게임(인간 존엄성) → 팁만 받고 스스로 수학 풀기(기술의 합목적성) → 참고만 하고 스스로 그리기(Canvas2D) → 우리 손으로 공평한 팀 편성(사회 공공선) → 노아에게 따뜻한 말 건네기 → **나의 약속 3가지 선택 + 다짐 서명 크게 쓰기**
6. **마무리** — 수미상관 O/X ② 재질문 → 공존·상생 메시지 → **약속 헌장 PNG 다운로드**(Q1~Q4 + 서명, localStorage 연동) → 쿠키 씬 → THE END

## 🏫 공간 구성 (모두 분리된 3D 월드)
`집` · `월드맵(마을)` · `교실` · `운동장` · `복도` — 각 공간은 별도의 씬으로 빌드되며 퀘스트에 따라 이동이 잠금/해제됩니다.

## 📔 도덕 수첩 (9개 원칙 수집 시스템)
| 세트 | 의미 | 수집 위치 |
|---|---|---|
| 🤖 로봇 3원칙 | 로봇이 지킬 약속 | 1~2단계 등굣길 발판 (점프) |
| 🧭 인공지능 윤리 3대 원칙 | 만드는 사람·사회가 지킬 약속 | 3단계 교실·운동장·복도 |
| 📱 인공지능 로봇에 대한 윤리 원칙 | 사용하는 내가 지킬 약속 | 5단계 교실·운동장·복도 |

- 카드는 **점프 또는 클릭(터치)**으로 획득 → 원칙 전문 카드 팝업 → 수첩 자동 기록
- HUD 좌상단 `📔 n/9` 버튼과 메인 화면 `도덕 수첩` 버튼으로 열람 (미획득은 ??? + 힌트)
- 수집 기록은 **회차를 넘어 누적** 저장되어 재플레이 동기를 만듭니다 (`localStorage`)
- 공간을 떠날 때 놓친 카드가 있으면 한 번 알림 (강제 잠금 없음)

## 💾 노아의 기록 저장소 & 💬 잡담 시스템
- **기록 저장소**: 3단계 복도 사물함 위 기록 장치에서 노아의 **관찰 기록 로그 3장**(#001~#003)을 발견 — 감정 단어 없이 관찰·횟수·질문만 기록되어 있고, ⚠불일치 수치(21%→48%→91%)가 4단계 시스템 오류의 복선이 됨. **읽어야 거울5로 진행**(감정 빌드업 필수 코스). 5단계 복도에는 새 파일 #004(불일치 감소)가 도착
- **잡담 시스템**: 자유 이동 구간에서 노아에게 F로 말 걸기 — 첫 대화에서 '가장 좋아하는 것'을 물어보고 **기억**했다가 이후 대화에서 다시 언급 (3단계/5단계 대사 톤 분리)

## 💕→💙 관계 게이지 (HUD 좌상단)
- **3단계**: 노아 조립 순간부터 `💕 친밀도` 게이지 등장 — 거울 에피소드마다 상승(+n% 연출), 거울5에서 **100% 도달 직후 가짜 엔딩**으로 연결 (반전의 복선)
- **5단계**: 재시작과 함께 `💙 존중` 게이지로 전환되어 **1%부터 다시** — 존중 에피소드마다 상승, 서명 완료 시 100%

## 🛠 기술
- **Three.js r147** (CDN, 무료 라이브러리) — 로우폴리 3D 월드/캐릭터 전부 코드로 제작 (외부 에셋 0개)
- **Canvas 2D API** — 그림 그리기, 서명, 헌장 이미지 생성/다운로드
- **Vibration API** — 시스템 오류 연출 (지원 기기에서)
- **getUserMedia** — 얼굴 스캔 연출 (카메라 없는 PC용 '상상 스캔 모드' 포함)
- **localStorage** — 친구의 정의/O·X 답변/약속/서명 저장 → 헌장에서 호출
- **WebAudio** — 효과음 (외부 파일 없음)

## 📁 파일 구조
```
index.html          화면 뼈대 (메뉴/캐릭터선택/HUD/터치컨트롤)
css/style.css       전체 스타일
js/data.js          대사·문제·카드 등 모든 데이터
js/state.js         게임 상태 + localStorage
js/ui.js            대화창/선택지/퀘스트/큰글자 연출/조이스틱/효과음
js/notebook.js      📔 도덕 수첩 (원칙 카드 팝업 + 열람 UI)
js/characters.js    3D 캐릭터 빌더 (플레이어 남녀, 노아 3종, NPC)
js/world.js         5개 공간 씬 빌더 + 존/발판/비콘 시스템
js/player.js        이동/점프/중력/상호작용/3인칭 카메라
js/minigames.js     미니게임 8종 (스캔·수학·미술·분류·팀편성·칭찬·약속·서명)
js/endings.js       가짜 엔딩(반전)·7942 재시작·진짜 엔딩·헌장 생성
js/episodes.js      퀘스트/에피소드 진행 오케스트레이터
js/main.js          부팅/메뉴/캐릭터 선택/게임 루프
```
